
using UnityEngine;

public class GrabableWeapon : MonoBehaviour
{
    public bool futuristWeapon=false;
    public int typeWeapon;// tipo de arma
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
